import streamlit as str_app

str_app.set_page_config(
    page_title="نظام وزارة التربية",
    page_icon="🖥️",
    layout="wide"
)

str_app.title("مرحباً بك يا مصطفى في نظام التشغيل والدعم التقني")
str_app.success("✅ تم ربط وتشغيل الموقع بنجاح عبر الإنترنت!")
str_app.write("النظام يعمل الآن بكفاءة وجاهز لإضافة قواعد البيانات والجداول.")
